## 五子棋AI 逐步实现与优化方案（从零到强）

> 面向 `241880200.py` 中的 `Search` 类，按阶段逐步构建与优化，确保每一步都可运行、可验证、可回退。课程要求参考：[Project 2: 五子棋AI](https://www.lamda.nju.edu.cn/guolz/IntroAI/fall2025/project2.html)。本方案中的代码示例与注释均为中文，符合你的偏好[[memory:8644410]]。

### 总目标
- 在不修改环境 (`gomoku.py`) 的前提下，实现一个在 60 秒/步内尽量强的搜索 AI。
- 通过“候选走法控制 + 高质量评估 + 有效剪枝 + 稳定的时限控制”逐步逼近最强性能。

---

## 阶段 0：基线与环境自检
- 验证 `make_move(board)` 返回 `(row, col)` 且不越界/不占用已有棋位。
- 启动方式：
  - 人机对战：`python gomoku.py -m human`
  - 随机对战：`python gomoku.py -m random`
- 验收标准：无异常、能正常对弈、每步不超时（<60s）。

建议实现（非常简单）：
- 空棋盘走中心；否则随机一个合法空位（仅用于自检）。

---

## 阶段 1：启发式贪心（无搜索）
核心思想：在不做搜索的情况下，用简单启发式挑选当前“看起来最好”的点，先建立一个可用的“非随机”基础。

实现要点：
- 候选生成：仅考虑“已有棋子周围（切比雪夫距离≤2）”的空位；若棋盘很空，包含中心附近点。
- 快速特判：
  - 先看“我是否有一步致胜”；有则直接下。
  - 再看“对手是否有一步致胜”；有则优先堵。
- 打分（局部潜力）：
  - 估计该点能延展的最长连子 + 两端可延展性；
  - 中央偏好（距离中心近者略优）。

验收标准：
- 明显强于随机；中局能主动连子、遇见对手即胜点能有效防守。

---

## 阶段 2：一层搜索（极小化极大/Negamax，深度=1）
核心思想：尝试每个候选点，假设我在此落子，计算新局面的启发式分（阶段1的评估），选评分最高的着。

实现要点：
- 仍用阶段1的候选生成与评估函数；
- 只做一层“模拟落子 + 评估”，不考虑对手应对；
- 保留“立即胜/必堵”特判。

验收标准：
- 相比阶段1，在无明显即胜/即挡时更稳定地走向连子。

---

## 阶段 3：加入对手一手（深度=2）
核心思想：我走一步后，对手也会走一步（最优反制），使用 Negamax 框架最小化我方损失。

实现要点：
- Negamax 框架：`value = -negamax(next_board, depth-1)`；
- 加入 alpha-beta 剪枝，显著降低分支；
- 简单走法排序：把“立即胜/必堵/四/活三”提前。

验收标准：
- 对手的简单威胁可以提前感知并规避；强度>阶段2。

---

## 阶段 4：迭代加深 + 稳定时限
核心思想：从浅到深逐层搜索，随时受时限打断并回退至最近完成深度的最优解。

实现要点：
- 迭代加深循环：`for depth in 2..N`，每轮检查时间；
- 留足安全余量：例如环境限制60s，则算法内部限制在 ~58.5s；
- 根上缓存“上次完整深度的最佳着”作为回退。

验收标准：
- 在 60s/步 内稳定返回着法，深度在中后盘也能探索到 3~5（视分支因子而定）。

---

## 阶段 5：评估函数升级（模式评分）
核心思想：把“活四/冲四/活三/眠三/活二”等模式转换为分值；己方分 − 防守权重×对方分。

实现要点：
- 扫描行/列/两族对角线，转换为字符串，统计模式次数；
- 典型权重：
  - 五连：极大正负分（胜负）；
  - 活四：高分；冲四：次之；活三/眠三/活二依次递减；
- 防守权重 > 1（如 1.05~1.2），更注重压制对手威胁。

验收标准：
- 明显提升中后盘判断力；不会因局部贪心而忽视对手强威胁。

---

## 阶段 6：走法生成与排序优化
核心思想：控制分支因子是搜索强度的核心（比“纯深度”更重要）。

实现要点：
- 候选 = 邻域候选（≤2）+ 特判（立即胜/必堵）
- 排序优先级：
  1) TT/PV（若有）；2) 立即胜；3) 必堵；4) 形成四/活三；5) 兼顾中央与局部潜力。
- 中盘空位很多时限制候选上限（例如 top-K），时间紧时动态收缩。

验收标准：
- 同等时限下平均搜索深度更深，回合耗时更稳定。

---

## 阶段 7：置换表（Transposition Table）+ Zobrist
核心思想：相同局面重复出现时复用结果，提高重搜效率与排序质量。

实现要点：
- Zobrist 哈希：为每格/每方分配随机位串；
- 置换表项：`(depth, flag, value, best_move)`；flag ∈ {EXACT, LOWER, UPPER}；
- 注意随机数生成的类型边界（推荐 int64 上界后转 uint64，已在代码中修复）。

验收标准：
- 节点访问量显著降低，排序更准确，剪枝更积极。

---

## 阶段 8：剪枝细化（Killer/History/LMR）
核心思想：对“可能很差”的后续分支做减深或推迟，集中算力在强应走法上。

实现要点：
- Killer move：同深度反复造成剪枝的走法优先；
- History heuristic：历史上在剪枝中表现好的走法得分更高；
- LMR（Late Move Reduction）：对排序靠后的非强应走法减1深再验证。

验收标准：
- 在大中盘棋面上，单位时间探索深度再提升 0.5~1 层。

---

## 阶段 9：威胁扩展（可选，谨慎）
核心思想：在叶节点若存在“四/活三”等强威胁，只扩展这些“强制走法”，以较低代价提高战术敏感度。

实现要点：
- 仅在深度边界触发，且限定扩展分支数量；
- 若时间趋紧，直接关闭此扩展。

验收标准：
- 在逼和/取胜的短算路上更少漏算；无显著超时风险。

---

## 阶段 10：参数调优与防守偏置
可调参数建议：
- 候选邻域半径：`1~2`；
- 防守权重：`1.05~1.20`（对手威胁重一点通常更稳健）；
- 迭代加深最大深度上限：`6~8`（由机器性能决定）；
- 根窗（alpha/beta 初始窗）：可用小吸气窗逐步调整。

调优方法：
- 自对弈 + 对随机/贪心/旧版搜索多轮；
- 记录每步用时、探索深度、节点数（可选日志）。

---

## 阶段 11：测试与验收
测试矩阵：
- 棋盘大小：11×11（默认）、15×15（可选）；
- 对手：随机、人类、不同版本搜索；
- 起手：先手/后手；
- 用时：检查 60s 限制是否稳定满足。

回归检查清单：
- 立即胜/必堵永不漏；
- 不会返回非法落点；
- 时限内必有返回（超时有回退）。

---

## 最终版本应具备的能力
- 迭代加深 + Alpha-Beta 剪枝 + 高质量走法排序；
- 模式驱动的评估（双边）+ 防守偏置；
- 安全稳健的时限控制（58.5s 内返回）；
- 置换表（Zobrist）与基础启发式剪枝（Killer/History/LMR）。

---

## 开发落地建议（配合 `241880200.py`）
- 以分阶段的 feature flag 实现（便于A/B）：
  - `USE_MINIMAX`、`USE_AB_PRUNE`、`USE_ITER_DEEPENING`、`USE_TT`、`USE_LMR`、`ENABLE_THREAT_EXTENSION` 等；
  - 每次只开启一个新特性，验证通过再叠加。
- 提交节奏：每阶段产出可运行版本，记录关键参数与战力变化。
- 注释与文档：保持中文注释，重要权值在代码顶端集中可配。

---

## 小结
循序渐进的关键是：先控分支（候选/排序），再稳评估（模式/双边），配剪枝与时限控制，最后用置换表与启发式把同样的算力“花在刀刃上”。这样在课程要求与 60 秒限制下，能够稳定打出较强水平，同时具备良好的可维护性与可调性。


